# Environment files with API keys
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/

# Logs
*.log

# Node (for VS Code extension)
node_modules/

# Temporary files
tmp/
temp/
*.tmp

# PR-13: Progress anchor (per-task, project-local). Users may commit
# their own .claude-progress.txt if they want to, but the default
# is for it to stay local — it changes every session.
.claude-progress.txt

# ── Local-only (added for GitHub upload) ──────────────────────
# Agent working directory — user demo files, not core code
workspace/

# Claude Code local settings (per-machine state)
.claude/

# Cached index data
data/

# Stray experimental files
*.go

# Egg-info (build artifact)
coding_agent.egg-info/